name: 'loan_analytics'
version: '1.0.0'
config-version: 2

profile: 'loan_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  loan_analytics:
    +schema: public
    staging:
      +schema: public
    marts:
      +schema: public